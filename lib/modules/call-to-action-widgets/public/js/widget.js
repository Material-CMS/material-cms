apos.utils.widgetPlayers['call-to-action'] = function(el, data, options) {

  // Button color check
  var colorField = el.getElementsByClassName('btn-large')[0];
  applyBackgroundColorClass(colorField);


};
