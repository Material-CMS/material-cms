apos.utils.widgetPlayers['sections'] = function(el, data, options) {
  // Initialize Materialize Parallax
  var parallax = M.Parallax.init(el.querySelector('.parallax'), {
    // example-option: true
  });
};
