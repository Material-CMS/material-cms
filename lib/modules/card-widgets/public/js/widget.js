apos.utils.widgetPlayers['card'] = function(el, data, options) {

  // Initialize lightbox effect
  if (data.lightboxOn) {
     var lightboxImage = el.getElementsByClassName('lightbox')[0];
     ligthboxEffect(lightboxImage);
  }

};
