apos.utils.widgetPlayers['events'] = function(el, data, options) {
  // Initialize Materialize Floating Action Button
  var collapsibles = M.Collapsible.init(el.querySelector('.collapsible'), {
    // example-option: true
  });
};
